/**
 * Application [flux] constants
 *
 */

const keyMirror = require('fbjs/lib/keyMirror'); // fixme: weak dependecy


module.exports = keyMirror({
    CART_ADD: null,
    CART_REMOVE: null,
    CART_VISIBLE: null,
    FETCH_DATA: null
});
